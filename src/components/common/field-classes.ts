import styles from './Field.module.css';

/**
 * Shared form-control class names.
 *
 * Exported from a plain module rather than from `Field.tsx` so that file exports only components.
 */
export const fieldControlClass = styles.control;
export const fieldRowClass = styles.row;
