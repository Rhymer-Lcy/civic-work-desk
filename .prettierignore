node_modules/
dist/
coverage/
playwright-report/
test-results/
_private_reference/
_review_packages/
package-lock.json
*.tsbuildinfo
public/icons/

# Generated by scripts/generate-css-module-types.mjs; shape is fixed and checked by that
# script's own --check mode in CI.
*.module.css.d.ts

# The UOS release bundle's `app/` is a copy of `dist/`: build output, not source. The bundle's
# scripts and documents are still formatted — only the staged build is excluded.
#
# Two depths, because the two bundle generations nest differently: the RC bundles stage at
# release/<bundle>/app/, while the final release stages at release/uos20/<release-name>/app/ so that
# the archive unpacks into a directory named after the release.
release/*/app/
release/*/SHA256SUMS.txt
release/*/*/app/
release/*/*/SHA256SUMS.txt
