# CivicWorkDesk UOS RC1.1 — 验收结果表

在目标机上直接编辑本文件，或复制为 `acceptance/RESULTS.md` 后填写。
`collect-results.sh` 会把这两个文件名中存在的那个收进结果包。

每一行请填 **PASS / FAIL / N/A**，失败的旁边写下实际现象。空着不填等同于「没测」，
不等同于通过。

---

## 0 · 本次测试的基本信息

| 项                                            | 填写 |
| --------------------------------------------- | ---- |
| 测试日期                                      |      |
| 外层压缩包 `sha256sum -c` 结果                |      |
| 包内 `verify-files.sh` 结果                   |      |
| 探针报告文件名                                |      |
| 浏览器「关于」页显示的版本号                  |      |
| 浏览器内核版本（about 页 / UA 中的 Chrome/…） |      |
| 是否全程未使用 sudo                           |      |

---

## 1 · 环境探针要点（从探针报告抄过来）

| 项                                               | 实际值 |
| ------------------------------------------------ | ------ |
| `/etc/os-release` 的 `PRETTY_NAME`               |        |
| `uname -m`                                       |        |
| 内核版本                                         |        |
| `XDG_CURRENT_DESKTOP`                            |        |
| `python3` 版本                                   |        |
| `busybox` 版本                                   |        |
| busybox 是否有 `httpd` 小程序                    |        |
| `busybox httpd --help` 是否列出 `-c`（配置文件） |        |
| `xdg-settings get default-web-browser`           |        |
| `xdg-user-dir DESKTOP`                           |        |
| 可用的哈希工具                                   |        |
| 端口 8765 初始状态                               |        |

---

## 2 · 服务候选 A：BusyBox httpd

| 检查项                                    | 结果 | 备注                   |
| ----------------------------------------- | ---- | ---------------------- |
| 能启动                                    |      |                        |
| 接受 `-c httpd.conf`                      |      | 若不接受，记录报错原文 |
| 只监听 `127.0.0.1:8765`                   |      | 贴 `ss -ltn` 的那一行  |
| `/` 返回 `text/html`                      |      |                        |
| 入口 JS 的 Content-Type                   |      | **填实际值**           |
| 入口 CSS 的 Content-Type                  |      | **填实际值**           |
| `/sw.js` 的 Content-Type                  |      |                        |
| `/manifest.webmanifest` 的 Content-Type   |      |                        |
| `/deployment-health.json` 的 Content-Type |      |                        |
| 遍历探测 `/../SHA256SUMS.txt` 断言        |      | 必须「通过」           |
| 遍历探测 `/%2e%2e/SHA256SUMS.txt` 断言    |      | 必须「通过」           |
| 遍历探测 `/..%2fSHA256SUMS.txt` 断言      |      | 必须「通过」           |
| `/assets/` 目录列表被拒绝                 |      | 404 / 403              |
| 响应证据文件名                            |      | `.runtime/headers-…`   |
| 服务端日志确实收到 `/../` 原始路径        |      | 贴日志那一行           |
| 缓存相关响应头（有什么写什么）            |      |                        |
| 停止 / 重启正常                           |      |                        |
| 端口被占用时是否明确失败                  |      |                        |

## 2b · 服务候选 B：Python stdlib（仅在 A 不合格时填写）

| 检查项                              | 结果 | 备注             |
| ----------------------------------- | ---- | ---------------- |
| 能启动（python3 3.7.3）             |      |                  |
| 只监听 `127.0.0.1:8765`             |      |                  |
| `.js` 的 Content-Type               |      |                  |
| `/sw.js` 的 Content-Type            |      |                  |
| 入口 CSS 的 Content-Type            |      |                  |
| 三个遍历探测断言                    |      | 必须全部「通过」 |
| `/assets/` 目录列表被拒绝           |      |                  |
| `Cache-Control`：index.html / sw.js |      | 期望 `no-cache`  |
| `Cache-Control`：`/assets/*`        |      | 期望 `immutable` |
| 端口被占用时是否明确失败            |      |                  |

**为什么测了 B**（只在测了的情况下填）：A 具体哪一条不合格 ____________________

---

## 3 · 打开方式

| 检查项                                     | 结果 | 备注           |
| ------------------------------------------ | ---- | -------------- |
| `xdg-open` 打开的是 360 浏览器             |      | 实际打开了什么 |
| 打开后地址栏显示 `http://127.0.0.1:8765/`  |      | **逐字核对**   |
| 直接调用 360 可执行文件也能打开            |      |                |
| 使用的是用户原有浏览器配置（没有新建配置） |      |                |

---

## 4 · 应用启动

| 检查项                 | 结果 | 备注           |
| ---------------------- | ---- | -------------- |
| 无白屏                 |      |                |
| 控制台无红色报错       |      | 有则贴出原文   |
| 中文显示正常           |      |                |
| 六个导航目标都能打开   |      |                |
| 布局在本机分辨率下可用 |      | 记录实际分辨率 |

实际屏幕分辨率：____________

---

## 5 · 浏览器平台能力（Console 输出）

| 检查项                   | 结果 | 实际输出                       |
| ------------------------ | ---- | ------------------------------ |
| `location.origin`        |      | 必须是 `http://127.0.0.1:8765` |
| IndexedDB 可用           |      |                                |
| Service Worker 注册数    |      |                                |
| Cache Storage keys       |      |                                |
| `crypto.subtle` 类型     |      | 必须是 `object`                |
| `crypto.randomUUID` 类型 |      | 必须是 `function`              |

---

## 6 · 核心业务流程（测试数据）

| 步骤             | 结果 | 备注 |
| ---------------- | ---- | ---- |
| 新增工作记录     |      |      |
| 编辑工作记录     |      |      |
| 添加进展         |      |      |
| 新增荣誉         |      |      |
| 荣誉关联工作记录 |      |      |
| 搜索 / 筛选      |      |      |
| 台账显示         |      |      |
| 报告预览         |      |      |

---

## 7 · 持久化

| 步骤                       | 结果 | 备注         |
| -------------------------- | ---- | ------------ |
| 刷新页面后数据仍在         |      |              |
| 关闭并重开浏览器后数据仍在 |      |              |
| 停止并重启服务后数据仍在   |      |              |
| 重启工作站后数据仍在       |      | 未做则填 N/A |

如果某一步数据消失，当时地址栏的完整内容：____________________

---

## 8 · 备份与还原

| 步骤                   | 结果 | 备注     |
| ---------------------- | ---- | -------- |
| 导出 JSON 备份         |      | 文件名   |
| 备份文件确实下载到本地 |      | 下载目录 |
| 修改数据后执行完整还原 |      |          |
| 还原后状态符合预期     |      |          |

---

## 9 · 报表

| 步骤                    | 结果 | 备注           |
| ----------------------- | ---- | -------------- |
| 导出 XLSX               |      |                |
| 导出 DOCX               |      |                |
| 用本机办公套件打开 XLSX |      | 套件名称与版本 |
| 用本机办公套件打开 DOCX |      |                |

---

## 10 · 离线

| 检查项                                          | 结果 | 备注       |
| ----------------------------------------------- | ---- | ---------- |
| 全流程无需联网                                  |      |            |
| Network 面板中所有应用请求都指向 127.0.0.1:8765 |      |            |
| 观察到的浏览器自身后台请求（与本应用无关）      |      | 有就记下来 |

---

## 11 · 性能主观观察（`--disable-gpu-compositing`）

| 场景               | 流畅 / 可接受 / 卡顿 | 备注 |
| ------------------ | -------------------- | ---- |
| 工作列表滚动       |                      |      |
| 台账横向滚动       |                      |      |
| 台账纵向滚动与表头 |                      |      |
| 打开 / 关闭对话框  |                      |      |
| 展开长记录         |                      |      |
| 生成报告           |                      |      |
| 改变窗口大小       |                      |      |

---

## 12 · 结论与遗留问题

服务候选推荐（A / B，以及理由）：

```

```

遇到的问题、异常现象、看不懂的报错（越具体越好）：

```

```

测试过程中**没有**完成的步骤，以及原因：

```

```
