# 最终安装形态 — 目标机验收结果

填写说明：每一项写 **PASS** / **FAIL** / **N/A**。
**没有实际做的项目一定写 N/A，不要写 PASS。** 空着也比猜一个结论好。
FAIL 的项目请在「实际情况」里写清看到了什么。

> 本表的节次与 `FINAL_ACCEPTANCE.md` 一一对应。
> 注意两条顺序：第 14 节（收集证据）在第 15 节（卸载）**之前**；第 11 节（端口冲突）
> 结束时服务会被脚本恢复成运行状态。

---

## 基本信息

| 项目                 | 填写 |
| -------------------- | ---- |
| 测试日期（北京时间） |      |
| 安装包文件名         |      |
| 验收包文件名         |      |
| 版本号（releaseId）  |      |
| 工作站型号 / 架构    |      |
| 操作系统版本         |      |
| 浏览器版本           |      |
| 测试人               |      |

---

## 1. 两个包的校验（外层 + 包内清单）

**两个包都要校验。** 验收包本身也必须可信，否则后面脚本的结论都不可信。

| 项                                         | 结果 | 实际输出 |
| ------------------------------------------ | ---- | -------- |
| 安装包外层 `sha256sum -c` 显示 OK          |      |          |
| **验收包**外层 `sha256sum -c` 显示 OK      |      |          |
| 安装包解压后 `sha256sum -c SHA256SUMS.txt` |      |          |
| 验收包解压后 `sha256sum -c SHA256SUMS.txt` |      |          |

## 2. 安装

| 项                           | 结果 | 实际情况 |
| ---------------------------- | ---- | -------- |
| 全程未要求密码 / 未使用 sudo |      |          |
| 显示「安装完成」             |      |          |
| 显示「检查通过」             |      |          |
| 安装路径                     | —    |          |
| 安装后报告的版本号           | —    |          |

## 3. 菜单启动

| 项                                    | 结果 | 实际情况 |
| ------------------------------------- | ---- | -------- |
| 菜单里有「政务工作记录台」且有图标    |      |          |
| 打开的是 360 安全浏览器               |      |          |
| 地址栏逐字为 `http://127.0.0.1:8765/` |      |          |
| 没有白屏                              |      |          |
| Console 没有红色报错                  |      |          |

## 4. 重复启动（幂等）

| 项                             | 结果 | 实际情况（PID） |
| ------------------------------ | ---- | --------------- |
| 第一次 status 报告的 PID       | —    |                 |
| 再点一次菜单项后 status 的 PID | —    |                 |
| 两次 PID 相同，且没有报错      |      |                 |

## 5. 浏览器平台证据（手册第四节那段 JSON）

```json
（把 Console 输出的 JSON 原样粘贴在这里）
```

| 项                             | 结果 | 实测值 |
| ------------------------------ | ---- | ------ |
| `location.origin`              |      |        |
| `'indexedDB' in window`        |      |        |
| `'serviceWorker' in navigator` |      |        |
| `'caches' in window`           |      |        |
| `typeof crypto.subtle`         |      |        |
| `typeof crypto.randomUUID`     |      |        |
| Service Worker 注册数量        |      |        |
| Cache Storage 的 key 列表      |      |        |

## 6. 核心流程（全部使用假数据）

| 项           | 结果 | 实际情况 |
| ------------ | ---- | -------- |
| 新建工作记录 |      |          |
| 编辑工作记录 |      |          |
| 添加进展     |      |          |
| 新建荣誉     |      |          |
| 荣誉关联工作 |      |          |
| 搜索 / 筛选  |      |          |
| 台账页       |      |          |
| 报表预览     |      |          |

## 7. 持久化

| 项                                      | 结果 | 实际情况 |
| --------------------------------------- | ---- | -------- |
| 刷新页面后数据仍在                      |      |          |
| 完全关闭并重开浏览器后数据仍在          |      |          |
| 停止再启动本地服务后数据仍在            |      |          |
| **重启整台工作站后数据仍在**            |      |          |
| 重启后地址仍为 `http://127.0.0.1:8765/` |      |          |

> 重启这一项如果没做，请写 **N/A**。

## 8. 备份与恢复

| 项                       | 结果 | 实际情况 |
| ------------------------ | ---- | -------- |
| 导出 JSON 备份           |      |          |
| 备份文件所在目录         | —    |          |
| 改动数据后用备份恢复     |      |          |
| 恢复结果与导出时完全一致 |      |          |

## 9. 报表

| 项                | 结果 | 实际情况 |
| ----------------- | ---- | -------- |
| 导出 XLSX         |      |          |
| XLSX 能在本机打开 |      |          |
| 导出 DOCX         |      |          |
| DOCX 能在本机打开 |      |          |

## 10. 离线

| 项                     | 结果 | 实际情况 |
| ---------------------- | ---- | -------- |
| 断网后核心流程全部正常 |      |          |

## 11. 端口冲突（脚本）

| 项                         | 结果 | 实际情况 |
| -------------------------- | ---- | -------- |
| 启动器明确报出端口被占用   |      |          |
| **没有**结束占用端口的进程 |      |          |
| **没有**改用别的端口       |      |          |
| **没有**打开浏览器         |      |          |
| 脚本结束时已恢复正常服务   |      |          |
| 恢复后健康检查通过         |      |          |
| 脚本最后输出               | —    |          |

## 12. 升级与回退（只有两个版本时才做）

| 项                       | 结果 | 实际情况 |
| ------------------------ | ---- | -------- |
| 升级后 status 显示新版本 |      |          |
| 升级后原有假数据仍在     |      |          |
| 升级后地址不变           |      |          |
| 回退后显示旧版本         |      |          |
| 回退后假数据仍在         |      |          |

## 13. 状态与停止命令

| 项                               | 结果 | 实际情况 |
| -------------------------------- | ---- | -------- |
| status 输出是中文且能看懂        |      |          |
| stop 能停止服务                  |      |          |
| 停止后 status 正确报告「未运行」 |      |          |
| 之后已把服务重新启动起来         |      |          |

## 14. 收集证据（**在卸载之前**）

| 项                                                 | 结果 | 实际情况 |
| -------------------------------------------------- | ---- | -------- |
| `collect-results.sh` 在服务运行状态下执行          |      |          |
| 生成的文件名                                       | —    |          |
| 文件里能看到版本号与健康检查结果（不是「未安装」） |      |          |
| 已把该文件另存一份到验收包目录之外                 |      |          |

## 15. 卸载（**最后一步**）

| 项                                     | 结果 | 实际情况 |
| -------------------------------------- | ---- | -------- |
| 菜单项消失                             |      |          |
| 程序目录被删除                         |      |          |
| 明确提示浏览器中仍保留数据             |      |          |
| 导出的 JSON / XLSX / DOCX 仍在下载目录 |      |          |
| `post-uninstall-check.sh` 最后输出     | —    |          |
| 生成的卸载后检查文件名                 | —    |          |

## 16. 主观体验

| 项           | 流畅 / 可接受 / 卡顿 | 说明 |
| ------------ | -------------------- | ---- |
| 工作列表滚动 |                      |      |
| 台账滚动     |                      |      |
| 报表生成     |                      |      |
| 启动到可用   |                      |      |

## 17. 其他异常

（没有就写「无」。有的话请写清：在哪一步、看到什么、Console 里的报错原文。）

---

## 回传清单

- [ ] 本文件（填好的）
- [ ] `civic-work-desk-final-results-<时间>.txt`（第 14 节，**卸载前**收集的）
- [ ] `civic-work-desk-post-uninstall-<时间>.txt`（第 15 节）
- [ ] 第 5 节那段 JSON
- [ ] 端口冲突脚本的完整输出
- [ ] 失败项的截图（**只有失败时才需要**）

> 回传前请确认：文件里没有真实业务数据、没有浏览器历史或 Cookie、没有你的个人文档。
> 账号名如不希望外传，可以自行替换成 `user`。
